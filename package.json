{
  "name": "jumpsso",
  "version": "0.1.0",
  "changelog": "新增映射功能，更灵活的登录方式",
  "scripts": {
    "install": "npm i --prefix=backend && npm i --prefix=frontend",
    "dev:frontend": "npm run dev --prefix=frontend",
    "dev:backend": "npm run dev --prefix=backend"
  }
}
