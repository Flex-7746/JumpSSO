// 环境
export const IS_DEV = import.meta.env.VITE_APP_ENV === 'fat'

// 默认语言
export const DEFULT_LANG = 'zh-Hans'

// 系统级页面路径
export const SYS_PATH = {
  index: '/',
  error: '/error',
  login: '/login',
}
